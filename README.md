# project1_testing
practice repo

terminal command to cement changes to repo-
as dev, update the message before commanding:

git add . ; if ($?) { git commit -m "Your commit message here" } ; if ($?) { git push }